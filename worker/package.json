{
  "name": "build-wizard-api",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker that proxies Build Wizard frontend requests to the Anthropic Claude API.",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.80.0"
  }
}
